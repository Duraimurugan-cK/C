#include<stdio.h>
#include<stdlib.h>
struct node
{
    int data;
    struct node*next;
}*new;
void inorder(int data)
{
    int data;
    new->next=left;
    new->data=root;
    new->next=right;
}
void insert(int data)
{
    new=(struct node*)malloc(sizeof(struct node));
    new->data=data;
    new->left=NULL;
    new->right=NULL;
}
void delete(int del)str
{
    if()
}
void main()
{
    int choice;
    scanf("%d",&n);
    switch(choice)
    {
    case 1:
        int data;
        scanf("%d",&data);
        void insert(data)
        break;
    }
    case 2:
        {
            void inorder(data);
            break;
        }
    case 3:
        {
            int del;
            scanf("%d",&del);
            void delete(int del);
        }

}
